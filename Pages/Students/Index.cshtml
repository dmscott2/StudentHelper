@page
@model StudentHelper.Pages.Students.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Student List</h1>

<p>
    <a href="Students/Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].StudentFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].StudentLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].StudentEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student[0].RegistrationDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentEmail)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegistrationDate)
            </td>
             
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.StudentID }) |
                @Html.ActionLink("Details", "Details", new {  id=item.StudentID }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.StudentID })
            </td>
        </tr>
}
    </tbody>
</table>